package model;

import model.listeners.LightListener;

public class LightDetector {

	public void registerListener(LightListener listener){
		/*
		 * add lsitener to listeners
		 */
	}
	
	public void lookForLine(){
		/*
		 * Loop forever
		 * 		while no line detected
		 * 			delay
		 * 		notify line under light
		 * 		while line detected
		 * 			delay
		 */
	}
	
}
